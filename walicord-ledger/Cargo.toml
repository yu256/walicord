[package]
name = "walicord-ledger"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"], optional = true }
thiserror = "2.0.17"
walicord-domain = { path = "../walicord-domain" }

[features]
canonical-attachment-codec = ["dep:serde"]
legacy-discord-ledger = []
test-fixtures = []

[lints]
workspace = true
